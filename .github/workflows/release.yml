name: release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  id-token: write  # for PyPI Trusted Publisher (OIDC)

jobs:
  build-and-publish:
    runs-on: ubuntu-latest
    environment:
      name: pypi
      url: https://pypi.org/p/sharepoint-mcp
    steps:
      - uses: actions/checkout@v6

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Install uv
        uses: astral-sh/setup-uv@v7

      - name: Install package + dev dependencies
        run: uv sync --extra dev

      - name: Run unit + integration tests (gate)
        run: ./tests/run_tests.sh

      - name: Build wheel + sdist
        run: uv build

      - name: Publish to PyPI (OIDC Trusted Publisher)
        # Requires PyPI Trusted Publisher configured for this repo +
        # the `pypi` GH environment. See docs/RELEASING.md for setup.
        run: uv publish

  github-release:
    runs-on: ubuntu-latest
    needs: build-and-publish
    steps:
      - uses: actions/checkout@v6
      - name: Create GitHub Release
        uses: softprops/action-gh-release@v3
        with:
          generate_release_notes: true
